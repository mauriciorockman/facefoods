section
	h4.titulo Resumo Do Seu Pedido
	.container
		.row(ng-repeat="order in shoppingCart.orders")
			.col.container-pedido
				.row
					.col.text-right
						//- TODO: Animação de apagar pedido 
						.btn-apagar-prato.fa.fa-times-circle(ng-click="deleteOrder(order.id)")
				.row
					.col
						.card-pedido
							.row
								.col-3
									button.btn-qtd(ng-click="incrementOrder(order.id)")
										i.fa.fa-plus
									.prato-qtd {{order.qty}}
									button.btn-qtd(ng-click="decrementOrder(order.id)")
										i.fa.fa-minus
								.col-9
									order(order='order')
									.row
										.col-9
											.total-produto Subtotal
										.col-3.text-left.nopadding
											.total-produto-preco R${{subTotal(order)}}
	.container.mt-4
		hr.hrCool
		.row
			.col
				h4.display.text-center Total: R${{total()}}
		.row
			.col
				button.ml-5.keepBuying.red(ui-sref="Menu", type="button") Continue comprando
				p.text-center.my-1 ou
				button.ml-5.keepBuying.gray(type="button") Confirme o Pedido
				